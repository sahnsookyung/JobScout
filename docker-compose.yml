# Base services (infrastructure anchors)
# Run: docker-compose -f docker-compose.yml -f docker-compose.microservices.yml up -d
#
# The base infrastructure (postgres, redis) is included automatically.
# For microservices, use: docker-compose -f docker-compose.yml -f docker-compose.microservices.yml up -d

include:
  - docker-compose.base.yml

services: {}
